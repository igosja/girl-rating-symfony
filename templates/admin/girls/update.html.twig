{% extends 'admin/base.html.twig' %}

{% block title %}Update girl{% endblock %}

{% block body %}
    <h1 class="text-center">Update girl</h1>
    <ul class="list-inline text-center">
        <li class="list-inline-item">
            <a class="btn btn-default" href="{{ path('admin_girls') }}">List</a>
        </li>
        <li class="list-inline-item">
            <a class="btn btn-default" href="{{ path('admin_girl_view', {'id': girl.id}) }}">View</a>
        </li>
    </ul>

    {{ include('admin/girls/_form.html.twig') }}
{% endblock %}
